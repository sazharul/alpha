// This file defines the roles initially, but can be updated dynamically
const roles = [
    'Admin',
    'Agent',
    'Merchant',
    'Staff',
    // You can fetch additional roles dynamically here if needed
];

export default roles;